import React from "react";

import FlexPanel from "../components/flexPanel/FlexPanel";

const Home = () => {
  return (
    <main className="nav-home">
      <FlexPanel />
    </main>
  );
};

export default Home;
